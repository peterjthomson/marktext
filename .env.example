# MarkText Build Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# macOS Code Signing & Notarization (Local Builds)
# =============================================================================

# Keychain profile name for macOS notarization
# Create with: xcrun notarytool store-credentials "AC_PASSWORD" --apple-id "YOUR_APPLE_ID" --team-id "YOUR_TEAM_ID"
APPLE_KEYCHAIN_PROFILE=AC_PASSWORD

# Optional: Skip notarization for development builds (set to any value to skip)
# SKIP_NOTARIZATION=1

# =============================================================================
# GitHub Actions (CI/CD) - Not required for local builds
# =============================================================================
# These are configured as GitHub Secrets in the repository settings:
# - APPLE_ID: Your Apple Developer account email
# - APPLE_APP_SPECIFIC_PASSWORD: App-specific password from appleid.apple.com
# - APPLE_TEAM_ID: Your Apple Developer Team ID
# - MAC_CERTS: Base64-encoded .p12 certificate
# - MAC_CERTS_PASSWORD: Password for the .p12 certificate
